meta {
  name: CREATE
  type: http
  seq: 4
}

post {
  url: {{httpHost}}/{{appContext}}/{{apiBase}}/tenants
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "code": "myrealm",
    "label": "My Realm",
    "status": "ACTIVE"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
